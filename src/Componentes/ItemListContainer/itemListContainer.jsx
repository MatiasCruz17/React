const ItemListContainer = ({ greeting }) => {
    return (
        <div style={{ padding: "20px", textAlign: "center", fontSize: "1.5rem" }}>
            <h2>{greeting}</h2>
        </div>
    );
};

export default ItemListContainer;